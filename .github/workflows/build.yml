name: Build DLL

on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Build 32-bit DLL
        run: |
          cl /LD /EHsc /FeDummy.dll /machine:X86 Dummy.cpp
          dir

      - name: Upload DLL
        uses: actions/upload-artifact@v4
        with:
          name: Dummy
          path: Dummy.dll
